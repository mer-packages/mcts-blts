How to run:
==============
Recommend to execute tests on Web UI via Testkit manager:
    Install testkit-manager, using webUI to select and start up your tests.
You also could run tests on command line via testkit-lite:
    Install testkit-lite, run "testkit-lite -M -f /usr/share/mnts-tracker-tests/tests.xml". About more function, check "man testkit-lite".

Common Install
==============
    ./autogen
    ./configure prefix=/usr
    make && make install

Package Install
===============
    ./pack.sh
    rpm -ivh <packagename>-<version>-<release>-<arch>.rpm

