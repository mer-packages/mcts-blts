How to run:
==============
Recommend to execute tests on Web UI via Testkit manager:
    Install testkit-manager, using webUI to select and start up your tests.
You also could run tests on command line via testrunner-lite:
    Install testrunner-lite, run "testrunner-lite -f /usr/share/mnts-packagekit-tests/tests.xml". About more function, check "man testrunner-lite".

Common Install
==============

Package Install
===============
    ./pack.sh
    rpm -ivh <packagename>-<version>-<release>-<arch>.rpm

